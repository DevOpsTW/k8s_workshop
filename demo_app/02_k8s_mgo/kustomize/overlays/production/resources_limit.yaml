apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo
spec:
  template:
    spec:
      containers:
        - name: mongodb
          resources:
            limits:
              cpu: "2000m"
              memory: "1000Mi"
