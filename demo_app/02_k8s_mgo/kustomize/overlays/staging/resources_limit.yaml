apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo
spec:
  template:
    spec:
      containers:
        - name: monbodb
          resources:
            limits:
              cpu: "1000m"
              memory: "1000Mi"
